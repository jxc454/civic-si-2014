overwrite: true
schema: http://localhost:4000/graphql
config:
  namingConvention: keep
  withHooks: true
generates:
  src/generated/queries.d.tsx:
    documents:
      - src/queries.gql
      - src/mutations.gql
    plugins:
      - typescript
      - typescript-operations
      - typescript-react-apollo